To start this script you need to pass 2 arguments  
`--path` - the path to file with data  
`--normalization` - type of normalization (z_score available only)  

An example:  
`python3 --path=data/test.tsv --normalization=z_score`

